/**
 * @description Adds 2 numbers
 * @param a number
 * @param b number
 * @returns number
 */
export const addInt = (a: number, b: number) => a + b;

export const minusInt = (a: number, b: number) => a - b;